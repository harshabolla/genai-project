print("hello world")
#python -m venv venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.venv\Scripts\Activate.ps1
#pip install -U langchain langchain-community langchain-google-genai streamlit python-dotenv
#streamlit run rag.py
pip install onnxruntime==1.16.0
#streamlit run rd.py
pip install -U :class:`~langchain-unstructured
pip install pymupdf
pip install pymupdf
 pip install "unstructured[docx]"
 
#git --version
# https://github.com/harshabolla/genai-project
first time 
git init
git remote add origin <your-repo-URL>
echo .venv/ > .gitignore
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main

after changes 
git status
git add .
git commit -m "Meaningful message"
git push



second pc
git clone <your-repo-URL>
cd <repo-folder>

Edit your code in VS Code.

When ready to save changes to GitHub:

bash
Copy
Edit
git status
git add .
git commit -m "My changes on second PC"
git push

git pull

git clone <repo-url>
cd <repo>
python -m venv .venv
.venv\Scripts\activate  (or source .venv/bin/activate)
pip install -r requirements.txt
 pip install --upgrade pymupdf==4.0.0 unstructured==0.12.2 onnxruntime==1.16.0  unstructured-inference==0.7.11+41+